

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: 'export',
    distDir: 'dist',
    images: {
      unoptimized: true,
    },
  };
  
  // eslint-disable-next-line no-undef
  module.exports = nextConfig;
  